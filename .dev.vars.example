# Local dev secrets for `next dev` / `wrangler dev` (copy to `.dev.vars`, which is gitignored).
# Session-cookie HMAC signing key (DEVPLAN §4). In production set it with:  wrangler secret put SECRET
SECRET="change-me-to-a-long-random-string"
